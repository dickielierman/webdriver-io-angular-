// const PageObj = require('../pages/sample.page');

describe('Test Suite', () => {
  beforeEach(async () => {
    await browser.url('');
  });
  afterEach(async () => {
    // await browser.pause(5000);
  });
  it('should', async () => {
    // temp
  });
});
